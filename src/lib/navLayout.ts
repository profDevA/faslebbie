/** Figma holistic nav bar height (2229:25243 / 1:128). Nav scrolls with the page (not sticky). */
export const NAV_H_PX = 82;
export const NAV_H = "h-[82px]";
/** Offset below the in-flow nav (e.g. fixed home intro layers at scroll 0). */
export const NAV_TOP = "top-[82px]";
/** Listing-page brighten pin — sticks to viewport top (nav is not sticky). */
export const STICKY_UNDER_NAV = "lg:sticky lg:top-0";
