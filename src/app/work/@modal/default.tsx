// Fallback for the `@modal` slot when no case study is intercepted (the /work
// index, or any hard navigation). Renders nothing so the grid shows alone.
export default function ModalDefault() {
  return null;
}
